import React from 'react';

const CheckInput = () => {

    const style = {
      display : "flex",
      flexdirection : "column",
      width : "200px"
    }

    // inputRef로 전달
    // formRef로 전달
    // 

  return (
    <form />
  );
};

export default CheckInput;